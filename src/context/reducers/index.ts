import ActionType from "types/ActionType";
import InitialState from "types/initialStates";

const reducer = (state: InitialState, { type }: ActionType) => {
  switch (type) {
    default:
      return state;
  }
};

export default reducer;
